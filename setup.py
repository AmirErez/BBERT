#!/usr/bin/env python
"""
Setup script for BBERT - BERT for Bacterial DNA Classification

This is a minimal setup.py for backward compatibility.
All configuration is in pyproject.toml (PEP 517/518).

For development installation:
    pip install -e .
    pip install -e .[dev]

For building:
    python -m build
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
